console.log(1);

//第一題
function hello() {
  return "Hello World!";
}

//第二題
function sayHi(name) {
  return `${name}，hello world`;
}

// 第三題
function add(num) {
  return num * 2;
}

//第四題
function addTwo(num1, num2) {
  const num0 = num1 + num2;
  return num0;
}

// 第五題
function addNumberAndString(num1, num2, num3) {
  const oneTwo = num1 + num2;
  const three = num3;
  return `第一加第二個參數加總為${oneTwo}，第三個參數為${three}`;
}
